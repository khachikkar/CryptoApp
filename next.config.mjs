/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['www.ixbt.com', 'dailyhodl.com'], // Add all required domains here
    },
};

export default nextConfig;

